<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>3.3.5</version>
    <relativePath/>
  </parent>

  <groupId>ar.edu.uade.pfi</groupId>
  <artifactId>pfi-backend</artifactId>
  <version>0.1.0-SNAPSHOT</version>
  <name>PFI Backend</name>
  <description>Standalone Spring Boot backend for the PFI AI service integration.</description>

  <developers>
    <developer>
      <email>xxxx.com</email>
      <name>Enzo Asplanatti</name>
      <id>enzo.asplanatti</id>
    </developer>
    <developer>
      <email>franciscofabrelo@gmail.com</email>
      <name>Francisco Fabrello</name>
      <id>fabrello.francisco</id>
    </developer>
  </developers>

  <properties>
    <java.version>17</java.version>
    <maven.version>3.8.3</maven.version>

    <postgresql.version>42.7.4</postgresql.version>
    <!-- 2.6.x es la linea de springdoc que acompana a Spring Boot 3.3.x. -->
    <springdoc-openapi.version>2.6.0</springdoc-openapi.version>
    <testcontainers.version>1.20.4</testcontainers.version>
    <wiremock-standalone.version>3.13.2</wiremock-standalone.version>

    <spotless-maven-plugin.version>2.43.0</spotless-maven-plugin.version>
    <maven-enforcer-plugin.version>3.4.1</maven-enforcer-plugin.version>
  </properties>

  <dependencies>
    <!-- General dependencies -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-webflux</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-validation</artifactId>
    </dependency>
    <dependency>
      <groupId>org.postgresql</groupId>
      <artifactId>postgresql</artifactId>
      <version>${postgresql.version}</version>
    </dependency>
    <!--
      Documentacion OpenAPI generada desde el propio codigo. Se prefiere que la
      referencia de la API salga de los controllers y DTOs reales y no de un documento
      escrito a mano, que envejece sin que nadie se entere.
    -->
    <dependency>
      <groupId>org.springdoc</groupId>
      <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
      <version>${springdoc-openapi.version}</version>
    </dependency>

    <!-- Test dependencies -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>junit-jupiter</artifactId>
      <version>${testcontainers.version}</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>postgresql</artifactId>
      <version>${testcontainers.version}</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.wiremock</groupId>
      <artifactId>wiremock-standalone</artifactId>
      <version>${wiremock-standalone.version}</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
      </plugin>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-enforcer-plugin</artifactId>
        <version>${maven-enforcer-plugin.version}</version>
        <executions>
          <execution>
            <id>enforce-versions</id>
            <goals>
              <goal>enforce</goal>
            </goals>
            <configuration>
              <rules>
                <dependencyConvergence />
                <!--
                  Rango cerrado, no un numero suelto: para el enforcer "17" significa
                  "17 o superior", asi que un JDK 21 o 25 pasaba la regla aunque el
                  proyecto declare 17. El contrato del proyecto es Java 17, y con
                  [17,18) un JDK distinto frena el build en la fase validate en vez de
                  aparecer mas tarde como una diferencia de bytecode o de runtime.
                -->
                <requireJavaVersion>
                  <version>[${java.version},18)</version>
                </requireJavaVersion>
                <requireMavenVersion>
                  <version>${maven.version}</version>
                </requireMavenVersion>
              </rules>
            </configuration>
          </execution>
        </executions>
      </plugin>

      <plugin>
        <groupId>com.diffplug.spotless</groupId>
        <artifactId>spotless-maven-plugin</artifactId>
        <version>${spotless-maven-plugin.version}</version>
        <configuration>
          <java>
            <googleJavaFormat />
            <removeUnusedImports />
            <formatAnnotations />
          </java>
        </configuration>
        <executions>
          <execution>
            <id>spotless-check</id>
            <phase>validate</phase>
            <goals>
              <goal>apply</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
